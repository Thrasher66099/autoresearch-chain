# spec/

Normative protocol specification documents for AutoResearch Chain.

## Status

Not yet populated. This directory will hold machine-readable or formally structured protocol specifications once they are separated from the current prose documents in `docs/`.

The current protocol specification lives at [docs/protocol-v0.2.md](../docs/protocol-v0.2.md).
